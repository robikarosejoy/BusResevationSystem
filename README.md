# BusResevationSystem
A Java-based Bus Reservation System built using Java Swing and MySQL. The application allows users to search buses, book seats, view bookings, and cancel reservations. JDBC is used for database connectivity with real-time seat availability updates through a simple GUI.
